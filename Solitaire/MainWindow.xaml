<Window x:Class="Spider.Solitaire.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Spider.Solitaire.ViewModel"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:CardViewModel}">
            <DataTemplate.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{Binding Path=Color}" />
                    <Setter Property="FontWeight" Value="Bold" />
                    <Setter Property="FontSize" Value="12" />
                </Style>
                <ScaleTransform x:Key="upsideDown" ScaleY="-1" />
            </DataTemplate.Resources>
            <Grid Height="120" Name="grid1" Width="90">
                <Rectangle Name="rectangle1" Stroke="Black" RadiusX="5" RadiusY="5" Fill="White">
                    <Rectangle.BitmapEffect>
                        <DropShadowBitmapEffect ShadowDepth="15" Direction="320" Opacity="0.5" />
                    </Rectangle.BitmapEffect>
                </Rectangle>
                <TextBlock Name="textBlock1" Text="{Binding Path=Face}" Margin="5,2,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" />
                <TextBlock Name="textBlock2" Text="{Binding Path=Suit}" Margin="0,2,5,0" HorizontalAlignment="Right" VerticalAlignment="Top" />
                <TextBlock Name="textBlock3" Text="{Binding Path=Face}" Margin="0,2,5,0" HorizontalAlignment="Right" VerticalAlignment="Bottom" LayoutTransform="{StaticResource ResourceKey=upsideDown}" />
                <TextBlock Name="textBlock4" Text="{Binding Path=Suit}" Margin="5,0,0,2" HorizontalAlignment="Left" VerticalAlignment="Bottom" LayoutTransform="{StaticResource ResourceKey=upsideDown}" />
                <TextBlock Name="textBlock5" Text="{Binding Path=Suit}" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Canvas Background="DarkGreen" Name="mainCanvas">
        <ContentControl Canvas.Top="10" Canvas.Left="10" Content="{Binding Path=AceOfHearts}" MouseDown="element_MouseDown" MouseMove="element_MouseMove" MouseUp="element_MouseUp" />
        <ItemsControl Canvas.Top="10" Canvas.Left="200" ItemsSource="{Binding Path=Pile}" MouseDown="element_MouseDown" MouseMove="element_MouseMove" MouseUp="element_MouseUp" />
    </Canvas>
</Window>
